**Slide 1: Introduction to Model Distillation**
*   **What is Model Distillation?** It is a training-time technique where a smaller "student" LLM learns by imitating the intermediate reasoning traces and final answers generated by a larger, more capable "teacher" LLM.
*   **The Motivation:** State-of-the-art reasoning models like the 671-billion-parameter DeepSeek-R1 are incredibly powerful but too computationally expensive for most to deploy. 
*   **The Goal:** Distillation transfers the rich reasoning behaviors of these massive teachers into smaller, highly cost-effective student models that can run on local hardware. 

**Slide 2: Hard vs. Soft Distillation**
*   **Soft Distillation:** The student is trained to match the teacher's full probability distribution (logits) over the entire vocabulary at each token generation step. While it provides richer information, it is highly resource-intensive and requires access to teacher logits.
*   **Hard Distillation:** The student is trained directly on the exact text tokens generated by the teacher. 
*   **Practical Choice:** Chapter 8 focuses exclusively on hard distillation because it acts like supervised fine-tuning on synthetic data and only requires the generated text, making it highly practical.

**Slide 3: Distillation vs. Reinforcement Learning (RLVR)**
*   **The RLVR Approach:** In Reinforcement Learning with Verifiable Rewards, the model generates an answer that is compared against a ground-truth reference by an automated verifier to calculate a reward signal.
*   **The Distillation Approach:** Supervision is much more direct. The teacher's step-by-step solution becomes the explicit target sequence that the student is trained to reproduce.
*   **The Advantage:** The teacher dataset can be generated ahead of time. Iterating on distillation is often much faster and cheaper than running active RLVR training loops.

**Slide 4: Dataset Generation and Formatting**
*   **The Dataset:** The pipeline uses 12,000 math problems from the MATH dataset, intentionally excluding the 500-problem test set (MATH-500) to prevent data leakage.
*   **Synthetic Solutions:** A strong teacher model (like DeepSeek-R1) generates the step-by-step reasoning traces and final answers for these 12,000 problems.
*   **Explicit Tagging:** The teacher's reasoning trace is enclosed inside **`<think>` and `</think>` tags**. This explicitly separates the "thinking" process from the final answer, making it easy for user interfaces to hide the verbose reasoning from end users.

**Slide 5: Tokenizing, Filtering, and Splitting**
*   **Tokenization:** The math prompt, teacher reasoning trace, and final answer are concatenated and wrapped in chat-template tokens (e.g., `<|im_start|>user`) to match standard prompting conventions. 
*   **Filtering Outliers:** Reasoning models can generate excessively long responses (with outliers reaching up to 42,000 tokens). To keep training feasible on smaller hardware, the dataset is strictly filtered to remove any sequences longer than 2,048 tokens.
*   **Data Splits:** The filtered dataset is shuffled and split into a large training set and a small validation set to track training progress.

**Slide 6: Loading the Base Model and Computing Loss**
*   **The Student Model:** The pipeline starts with a pre-trained base model (Qwen3 0.6B) rather than an RL-trained model. This ensures any reasoning improvements are directly attributable to learning from the distilled dataset.
*   **Answer-Only Cross-Entropy Loss:** The training signal relies on an **answer-only cross-entropy loss**, which calculates the negative average log-probability of predicting the correct next token.
*   **Masking the Prompt:** The loss is computed *only* on the teacher's answer tokens. The prompt tokens are intentionally ignored because the model's job is to produce the target answer, not to be penalized for failing to reconstruct the user's input prompt.

**Slide 7: The Distillation Training Loop**
*   **Training Epochs:** Unlike RLVR, distillation involves multiple epochs, meaning the model makes several complete passes through the fixed synthetic dataset to minimize its supervised loss.
*   **Standard Supervised Loop:** The training process follows standard conventions: shuffling training examples, computing the cross-entropy loss, backpropagating gradients, and updating model weights.
*   **Tracking Validation Loss:** The validation loss, computed on held-out examples without updating weights, is monitored closely. A steady drop in validation loss is the most reliable metric indicating that the student is successfully learning to generalize the teacher's reasoning patterns.

**Slide 8: Evaluating the Distilled Model**
*   **The Benchmark:** The newly distilled student model is evaluated on the held-out MATH-500 test set.
*   **Performance Jump:** The baseline Qwen3 0.6B model starts at a low 15.2% accuracy. After distillation on DeepSeek-R1 data, the accuracy improves significantly to 33.6%.
*   **Same-Family Distillation:** When distilling from a teacher within the same model family (e.g., a 235B Qwen3 model), the student achieves an impressive **45.0% accuracy**. Shared tokenizers, prompting conventions, and styles make the teacher's targets much easier for the student to successfully imitate.

**Slide 9: Future Directions for Reasoning Models**
*   **Refining the Recipe:** Future systems will continue to rely heavily on a combination of RLVR to train massive flagship models, followed by distillation to create efficient, deployable student models.
*   **Inference-Time Optimization:** Models will gain the ability to dynamically adjust their reasoning budget—knowing when to give a short answer and when to allocate deep, multi-step "thinking" for complex problems.
*   **Process Reward Models (PRMs):** Moving beyond outcome verification, PRMs aim to provide rich, step-by-step reward signals during training to teach more reliable reasoning strategies. 
*   **Multi-Reward Agent Training:** Reasoning models will increasingly act as the core engines in agent applications (like coding or tool use), optimizing multiple rewards simultaneously rather than just single-answer correctness.